import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Studio5-Mobile-App-New',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
